# هندسة الأداء — المرحلة الأولى

## قبل التعديل

كانت بيانات اللاعبين والجولة والأكاذيب والتخمينات والنتائج ووقت الاتصال داخل وثيقة واحدة. كل إجابة كانت Transaction على الوثيقة نفسها، لذلك تتنافس كتابات اللاعبين وHeartbeat وانتقالات المضيف على مورد واحد.

## بعد التعديل

### وثيقة الغرفة

تحتوي فقط على:

- المضيف والحالة العامة.
- الإعدادات والفئات.
- معرف الجولة الحالية.
- عدد اللاعبين.
- مواعيد النشاط والانتهاء.

### وثائق اللاعب

كل لاعب له وثيقة مستقلة، لذلك تحديث الجاهزية أو النقاط لا يعيد كتابة بيانات بقية اللاعبين.

### وثائق الإجابة

كل لاعب يكتب في مساره الحتمي:

```text
rounds/{roundId}/bluffs/{uid}
rounds/{roundId}/guesses/{uid}
rounds/{roundId}/revealReady/{uid}
```

لا توجد Transaction مشتركة عند إرسال الإجابة. تمنع Security Rules إنشاء إجابة ثانية أو تعديل الإجابة الأولى.

### المزامنة

`room-store.js` هو المصدر المشترك الوحيد لحالة Firestore. يشترك `app.js` و`game.js` محليًا في هذا المخزن بدل فتح مستمعين منفصلين لوثيقة الغرفة.

### التحميل

- الصفحة الرئيسية لا تستورد `game.js` حتى يدخل اللاعب غرفة.
- أجهزة اللاعبين لا تحمل بنك الأسئلة.
- جهاز المضيف يحمل `manifest.json` صغيرًا عند بدء اللعبة، ثم Shard واحدًا من الفئة المختارة عند الحاجة.
- صور الملف الشخصي داخل النافذة تستخدم Lazy Loading.

### Cache

- HTML: Network First بمهلة قصيرة.
- JS/CSS/Images: Stale While Revalidate.
- ملفات `questions/v8.5.0/*.json`: لا تدخل Precache، وتستخدم Cache First بعد أول طلب لأنها Versioned وImmutable.
- Firebase والطلبات الخارجية لا يعترضها Service Worker.

## ما لم يُنفذ بعد

هذه مرحلة أولى وليست البنية النهائية:

1. Presence ما زال مؤقتًا في Firestore؛ سينتقل إلى Realtime Database في المرحلة الثانية.
2. الغرفة العامة الفارغة تختفي من القائمة بعد انتهاء `expiresAt`، لكن حذف الوثيقة وما تحتها نهائيًا يحتاج وظيفة تنظيف من الخادم.
3. المضيف ما زال ينسق انتقالات المؤقت؛ النقل إلى Cloud Functions سيكون في المرحلة الثالثة.
4. الإجابة الصحيحة ما زالت موجودة في وثيقة الجولة المقروءة لأعضاء الغرفة؛ إخفاؤها نهائيًا يحتاج منطق خادم.
