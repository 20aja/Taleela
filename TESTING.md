# اختبار Taleela v8.10.0 بعد النشر

## اختبار البنك

1. افتح DevTools ثم Network.
2. ابدأ لعبة واختر فئة.
3. يجب أن تُطلب الملفات من `questions/v1/<category>/` فقط.
4. يجب ألا تظهر طلبات إلى أي مجلد `questions/v8.*`.
5. اختر فئة الأعلام وتأكد من ظهور SVG من `assets/countries/`.

## اختبار منع التكرار

لكل فئة 30 سؤالًا. لاختبار دورة كاملة:

1. استخدم الفئة نفسها في 30 جولة متتالية، ويمكن توزيعها على أكثر من مباراة في الغرفة نفسها.
2. سجل `questionId` لكل جولة من وثيقة الجولة في Firestore.
3. يجب أن تكون المعرّفات الثلاثون الأولى مختلفة.
4. لا يُسمح بالتكرار إلا عند الاختيار التالي بعد اكتمال الثلاثين.
5. نفذ Replay وتأكد من استمرار الدورة بدل بدئها من الصفر.
6. أعد المضيف أو اختبر انتقال المضيف وتأكد من بقاء منع التكرار داخل الغرفة.

## اختبار الأعلام في الدول

- في فئة `countries` لا يظهر العلم أثناء كتابة الكذبة أو التخمين.
- يظهر علم الدولة عند مرحلة الكشف فقط.
- في فئة `flags` يظهر العلم مع السؤال كما حدده ملف الأسئلة.
