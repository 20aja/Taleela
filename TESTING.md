# اختبار Taleela v8.1.0

## اختبار أساسي

1. افتح الموقع بعد النشر.
2. أنشئ غرفة جديدة.
3. أدخل لاعبًا ثانيًا.
4. اختر 5 فئات على الأقل وابدأ المباراة.
5. اختر فئة وانتظر ظهور السؤال.
6. أكمل الكذبة والتخمين والنتيجة والجولة التالية.

## اختبار تحميل الأسئلة

من DevTools → Network:

- عند الصفحة الرئيسية يجب ألا ترى تحميل آلاف الأسئلة.
- قبل بدء المباراة يجب ألا يُطلب أي ملف من `questions/v8.1.0/<category>/...`.
- عند اختيار فئة يظهر عادة طلب واحد لملف JSON صغير من تلك الفئة.
- إذا تكررت الفئة في جولة أخرى، يفترض أن يأتي الملف من Memory/Disk/Service Worker Cache بدل تحميل البنك كله.
- أجهزة اللاعبين غير المضيفين لا تحتاج إلى طلب ملفات بنك الأسئلة.

## اختبار عدم التكرار

- اختر فئة واحدة في عدة جولات عندما يسمح عدد الفئات/الجولات بذلك.
- يجب ألا يظهر ID السؤال نفسه مرتين في الغرفة ما دام في الفئة سؤال غير مستخدم.
- على جهاز المضيف، إنشاء مباريات جديدة سيستمر في استخدام أسئلة لم تظهر محليًا حتى تنتهي دورة الفئة.

## اختبار PWA

بعد أول استخدام لفئة ما:

1. اخرج من المباراة.
2. افتح اللعبة مجددًا.
3. يجب أن تبقى ملفات JSON التي استُخدمت في Cache.

## ملاحظة

الفئات التي تحتوي عددًا قليلًا من الأسئلة ستتكرر بعد نفاد كل محتواها. زيادة المحتوى الفعلي هي الحل الوحيد لتأخير هذا النوع من التكرار أكثر.
