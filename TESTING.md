# اختبار Taleela v8.9.0 بعد النشر

نفّذ هذا الاختبار بعد تحديث Netlify **ونشر `firestore.rules`**.

## Smoke Test سريع

1. أنشئ غرفة من جهاز A واجعل الجولات 30 والفئات فئة واحدة فقط.
2. انضم من جهاز B وتأكد أن بدء اللعبة يعمل.
3. تحقق أن هيدر اللعب يحتوي زر الخروج ورقم الجولة فقط، والمؤقت تحته وفي المنتصف.
4. في مرحلة اختيار الفئة، تحقق أن البطاقات بنفس أسلوب توزيع بطاقات إعداد الغرفة.
5. في مرحلة الكذبة، اكتب الإجابة الصحيحة حرفيًا وتأكد أن التنبيه يظهر فوق الحقل، ثم غيّرها إلى كذبة.
6. اجعل لاعبين يرسلان الكذبة نفسها. يجب أن تظهر مرة واحدة فقط في الخيارات، ولا يستطيع أي منهما اختيارها.
7. اجعل لاعبًا ثالثًا يختار الكذبة المشتركة. بعد الكشف يجب أن يظهر اسما صاحبيها ويحصل كل واحد على نقطة.
8. في نتائج الجولة، تحقق أن شريط النقاط والرقم يتحركان من النتيجة السابقة إلى الجديدة.
9. تأكد أن عداد النتائج السفلي غير موجود، وأن اللعبة تنتقل تلقائيًا بعد المهلة.
10. أكمل أو اختصر اختبار النهاية بحالة تعادل للتأكد من ترتيب: النقاط → الصحيح → الخداع، ثم ظهور أكثر من فائز عند التعادل الكامل.

## الشبكة وبنك الأسئلة

من أدوات المطور Network:

- قبل بدء اللعب لا يجب تحميل البنك كاملًا.
- عند الحاجة يجب أن تظهر طلبات من `questions/v8.9.0/<category>/...` فقط حسب الفئات المستخدمة.
- لا يفترض أن تحمل أجهزة اللاعبين العاديين كل ملفات الأسئلة.
