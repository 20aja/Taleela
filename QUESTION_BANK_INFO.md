# بنك أسئلة Taleela — v8.5.0

- عدد الفئات: **33**.
- إجمالي مداخل الأسئلة: **21,848**.
- عدد ملفات JSON المجزأة: **133 Shard**.
- حجم `manifest.json`: قرابة **7 KB** قبل ضغط HTTP.
- إجمالي ملفات البنك على الخادم: قرابة **4.9 MB** قبل ضغط HTTP، ولا تُحمّل دفعة واحدة.
- أكبر Shard أقل من **90 KB** تقريبًا قبل ضغط HTTP.

## طريقة التحميل

1. عند بدء المباراة على جهاز المضيف يُحمّل `manifest.json` فقط.
2. عند اختيار فئة، يحمّل المضيف Shard واحدًا من تلك الفئة عند الحاجة.
3. السؤال المختار وحده يُحفظ في Firestore ليظهر لجميع اللاعبين.
4. أجهزة بقية اللاعبين لا تحتاج إلى تحميل بنك الأسئلة.
5. Service Worker يحتفظ بملفات الأسئلة المستخدمة لأنها Versioned داخل `questions/v8.5.0/`.

## منع التكرار

المحرك يستخدم مستويين من الحماية:

- `usedQuestionIds`: يمنع تكرار نفس السؤال حرفيًا داخل الغرفة حتى تُستهلك كل مداخل الفئة.
- `usedFactKeys`: يفضّل عدم إعادة الحقيقة نفسها بصياغة أخرى قبل المرور على الحقائق الأخرى المتاحة.

إذا كانت الفئة صغيرة واخترها اللاعبون وحدها في لعبة طويلة حتى 30 جولة، يبدأ المحرك أولًا بالحقائق المختلفة، ثم يستخدم الصياغات البديلة غير المستخدمة قبل السماح بتكرار نفس Question ID.

كما يحتفظ جهاز المضيف بسجل محلي بين المباريات. عند استهلاك جميع مداخل الفئة يبدأ Cycle محلي جديد.

## ملاحظة حول مضاعفة المحتوى

المضاعفة تمت مع الحفاظ على سلامة البيانات بدل اختراع معلومات غير موثوقة:

- فئة **الأرقام** أضيفت لها مسائل جديدة فعلية ومتنوعة.
- فئة **الدول** أضيفت لها أنماط إضافية مثل الدولة من العاصمة، وعاصمة الدولة من العلم، مع إعادة صياغة بعض الأنماط القائمة.
- فئة **خمن اللاعب** توسعت بصيغ أدلة مختلفة اعتمادًا على بيانات اللاعب الموجودة أصلًا.
- بعض الفئات المعرفية الصغيرة تستخدم صياغات بديلة لحقائق موجودة إضافة إلى الخلط، لذلك عدد المداخل تضاعف، لكن ليس كل مدخل إضافي حقيقة معرفية جديدة مستقلة.

## البنية

```text
questions/
  v8.5.0/
    manifest.json
    general/001.json
    history/001.json
    numbers/001.json ...
    countries/001.json ...
    player_guess/001.json ...
    ...
```

لا يوجد اختيار مستوى صعوبة ظاهر للمستخدم؛ الخلط يتم داخل البنك نفسه.
